using System;

namespace uri1149
{
    class Program
    {
        static void Main(string[] args)
        {
            int A, N, soma;

            string[] vetor=new string[2];
            vetor=Console.ReadLine().Split(' ');
            A=int.Parse(vetor[0]);
            N=int.Parse(vetor[1]);

        }
    }
}
